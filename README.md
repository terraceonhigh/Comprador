# Comprador — gh-pages branch

GitHub Pages source. Auto-generated from `docs/WEBSITE-v0.4.0-DRAFT.md` on
the `master` branch via `build/render-website.py`. Do not edit files here
directly — edit the Markdown source and re-render.

The page currently lives at the draft URL (whichever the architect's
GitHub Pages settings point at). The red DRAFT border + sticky banner
make the not-final state obvious to any visitor; image placeholders
are pending real screenshots from v0.4.0 validation captures.
